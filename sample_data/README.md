# sample_data

An optional place for your own audio files while trying the command-line tools. Nothing here is
needed: the tests generate their own synthetic audio, and the GUI works on the music library and
FX samples folders set in its Configuration tab.

Do not commit audio files: keep them out of version control and only use files you have the
right to use.
