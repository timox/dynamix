"""
The one place DynaMix's version is written.

Read by setup.py, by the Windows build (packaging/dynamix.spec and
packaging/dynamix.iss, through packaging/build_windows.bat) and by the
Configuration tab, so a release only ever needs this file changed.
"""

VERSION = "0.2.0"
